{"ast":null,"code":"import { inject } from '@angular/core';\nimport { HttpClient, HttpParams } from '@angular/common/http';\nimport { throwError } from 'rxjs';\nimport { timeout, retry, catchError } from 'rxjs/operators';\nimport { environment } from '../environment';\nimport * as i0 from \"@angular/core\";\nconst API = environment.apiUrl;\nconst HTTP_TIMEOUT = 15000;\nconst RETRY_ATTEMPTS = 2;\nconst RETRY_DELAY = 1000;\nfunction dateRange(days = 30) {\n  const to = new Date();\n  const from = new Date();\n  from.setDate(from.getDate() - days);\n  return {\n    from: from.toISOString().split('T')[0],\n    to: to.toISOString().split('T')[0]\n  };\n}\nexport let ApiService = /*#__PURE__*/(() => {\n  class ApiService {\n    constructor() {\n      this.http = inject(HttpClient);\n    }\n    handleError(error) {\n      const message = error?.error?.message ?? 'Erro ao conectar ao servidor';\n      console.error('API Error:', message, error);\n      return throwError(() => new Error(message));\n    }\n    request(observable) {\n      return observable.pipe(timeout(HTTP_TIMEOUT), retry({\n        count: RETRY_ATTEMPTS,\n        delay: RETRY_DELAY\n      }), catchError(err => this.handleError(err)));\n    }\n    // Generic GET\n    get(endpoint, params) {\n      return this.request(this.http.get(`${API}${endpoint}`, {\n        params\n      }));\n    }\n    // Generic POST\n    post(endpoint, body) {\n      return this.request(this.http.post(`${API}${endpoint}`, body));\n    }\n    // Generic PUT\n    put(endpoint, body) {\n      return this.request(this.http.put(`${API}${endpoint}`, body));\n    }\n    // Generic PATCH\n    patch(endpoint, body) {\n      return this.request(this.http.patch(`${API}${endpoint}`, body));\n    }\n    // Generic DELETE\n    delete(endpoint) {\n      return this.request(this.http.delete(`${API}${endpoint}`));\n    }\n    // ── Campaigns ────────────────────────────────────────────────\n    getCampaigns(pagination) {\n      let params = new HttpParams();\n      if (pagination?.page) params = params.set('page', pagination.page.toString());\n      if (pagination?.limit) params = params.set('limit', pagination.limit.toString());\n      return this.request(this.http.get(`${API}/api/campaigns`, {\n        params\n      }));\n    }\n    getCampaign(id) {\n      return this.get(`/api/campaigns/${id}`);\n    }\n    // ── Metrics ──────────────────────────────────────────────────\n    getMetricsSummary(days = 30) {\n      const {\n        from,\n        to\n      } = dateRange(days);\n      const params = new HttpParams().set('from', from).set('to', to);\n      return this.get('/api/metrics/summary', params);\n    }\n    getCampaignMetrics(campaignId, days = 30) {\n      const {\n        from,\n        to\n      } = dateRange(days);\n      const params = new HttpParams().set('from', from).set('to', to);\n      return this.get(`/api/metrics/campaigns/${campaignId}`, params);\n    }\n    getCampaignAggregate(campaignId, days = 30) {\n      const {\n        from,\n        to\n      } = dateRange(days);\n      const params = new HttpParams().set('from', from).set('to', to);\n      return this.get(`/api/metrics/campaigns/${campaignId}/aggregate`, params);\n    }\n    getMetricsPaginated(pagination) {\n      let params = new HttpParams();\n      if (pagination?.page) params = params.set('page', pagination.page.toString());\n      if (pagination?.limit) params = params.set('limit', pagination.limit.toString());\n      return this.request(this.http.get(`${API}/api/metrics`, {\n        params\n      }));\n    }\n    getCampaignMetricsPaginated(campaignId, pagination) {\n      let params = new HttpParams();\n      if (pagination?.page) params = params.set('page', pagination.page.toString());\n      if (pagination?.limit) params = params.set('limit', pagination.limit.toString());\n      return this.request(this.http.get(`${API}/api/metrics/campaigns/${campaignId}`, {\n        params\n      }));\n    }\n    // ── Insights ─────────────────────────────────────────────────\n    getInsights(days = 30) {\n      const {\n        from,\n        to\n      } = dateRange(days);\n      const params = new HttpParams().set('from', from).set('to', to);\n      return this.get('/api/insights', params);\n    }\n    getCampaignInsights(campaignId, days = 30) {\n      const {\n        from,\n        to\n      } = dateRange(days);\n      const params = new HttpParams().set('from', from).set('to', to);\n      return this.get(`/api/insights/campaigns/${campaignId}`, params);\n    }\n    // ── Meta Accounts ─────────────────────────────────────────────\n    getAdAccounts() {\n      return this.get('/api/meta/accounts');\n    }\n    getMetaConnectUrl() {\n      return `${API}/api/meta/connect`;\n    }\n    static {\n      this.ɵfac = function ApiService_Factory(__ngFactoryType__) {\n        return new (__ngFactoryType__ || ApiService)();\n      };\n    }\n    static {\n      this.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n        token: ApiService,\n        factory: ApiService.ɵfac,\n        providedIn: 'root'\n      });\n    }\n  }\n  return ApiService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}